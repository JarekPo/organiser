import React from 'react';

import {Paper, Typography} from '@mui/material';

const Header = () => {
  return (
    <Paper>
      <Typography align='center' variant='h4' margin={1}>
        Organiser
      </Typography>
    </Paper>
  );
};

export default Header;
